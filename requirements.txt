# ---- core ----------------------------------------------------------------
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.26

# ---- audio ---------------------------------------------------------------
sounddevice>=0.4.6
soundfile>=0.12

# ---- speech to text (local) ---------------------------------------------
faster-whisper>=1.0

# ---- wake word (optional engine) ----------------------------------------
openwakeword>=0.6 ; sys_platform == "win32"
onnxruntime>=1.17

# ---- text to speech ------------------------------------------------------
pyttsx3>=2.90            # Windows SAPI, no extra install
edge-tts>=6.1            # free neural voices (needs internet)

# ---- LLM providers (install what you use; both are small) ---------------
anthropic>=0.40
openai>=1.40

# ---- tray icon -----------------------------------------------------------
pystray>=0.19
Pillow>=10.0

# ---- computer control ----------------------------------------------------
pyautogui>=0.9.54
pyperclip>=1.8
keyboard>=0.13.5
pygetwindow>=0.0.9
psutil>=5.9
