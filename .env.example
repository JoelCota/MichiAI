# Copy this file to ".env" and fill in only the keys you actually use.
# config.yaml refers to these by ${NAME} — the values never go in the config.

ANTHROPIC_API_KEY=sk-ant-...
OPENCODE_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
